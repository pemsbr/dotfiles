- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.hushlogin:

- create:
    - ~/Developer

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
